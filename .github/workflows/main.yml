on:
  workflow_call:
    secrets:
      api_key:
        description: 'dont look at me!!!!'
        required: true
  push:
    branches:
      - main

jobs:
  mimics_scan_job:
    runs-on: ubuntu-latest
    name: A job to scan with mimics
    steps:
      - name: mimics step
        env:
          INPUT_API-KEY: "did i work?"
        uses: ykim-r7/test-action@latest
        id: mimics
        with:
          blah-key: ${{ secrets.api_key }}
      - name: Get the output 
        run: echo ${{ steps.mimics.outputs.stdout }}
